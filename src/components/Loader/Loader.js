import React from 'react';
import './Loader.scss';

const Loader = () => {
    return (
        <div>
            <div className="reverse-spinner"></div>
        </div>
    );
};

export default Loader;
